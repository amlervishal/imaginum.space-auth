import { Syne } from 'next/font/google';

// Define the Syne font with same parameters as in prompt generator
export const syne = Syne({ 
  subsets: ["latin"],
  display: 'swap',
  variable: '--font-syne',
});
